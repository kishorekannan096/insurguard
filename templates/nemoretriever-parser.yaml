apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "insureguard.fullname" . }}-nemoretriever-parser
  labels:
    {{- include "insureguard.labels" . | nindent 4 }}
    app.kubernetes.io/name: nemoretriever-parser
spec:
  replicas: {{ .Values.nemoretrieverParser.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: nemoretriever-parser
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nemoretriever-parser
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
        {{- include "insureguard.securityContext" . | nindent 8 }}
      imagePullSecrets:
        - name: {{ include "insureguard.imagePullSecretName" . }}
      containers:
        - name: nemoretriever-parser
          image: {{ .Values.nemoretrieverParser.image }}
          securityContext:
            {{- include "insureguard.containerSecurityContext" . | nindent 12 }}
          env:
            {{- range $key, $val := .Values.nemoretrieverParser.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: NVIDIA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "insureguard.nvidiaSecretName" . }}
                  key: NVIDIA_API_KEY
            - name: NGC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "insureguard.nvidiaSecretName" . }}
                  key: NGC_API_KEY
          ports:
            - containerPort: {{ .Values.nemoretrieverParser.port }}
              name: http
          volumeMounts:
            - name: nim-cache
              mountPath: /opt/nim/.cache
            - name: dshm
              mountPath: /dev/shm
          resources:
            limits:
              nvidia.com/gpu: "1"
      volumes:
        - name: nim-cache
          emptyDir: {}
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.nemoretrieverParser.shmSize }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "insureguard.fullname" . }}-nemoretriever-parser
  labels:
    {{- include "insureguard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.nemoretrieverParser.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: nemoretriever-parser
    app.kubernetes.io/instance: {{ .Release.Name }}
