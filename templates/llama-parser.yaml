apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "insureguard.fullname" . }}-llama-parser
  labels:
    {{- include "insureguard.labels" . | nindent 4 }}
    app.kubernetes.io/name: llama-parser
spec:
  replicas: {{ .Values.llamaParser.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: llama-parser
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: llama-parser
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
        {{- include "insureguard.securityContext" . | nindent 8 }}
      imagePullSecrets:
        - name: {{ include "insureguard.imagePullSecretName" . }}
      containers:
        - name: llama-parser
          image: {{ .Values.llamaParser.image }}
          securityContext:
            {{- include "insureguard.containerSecurityContext" . | nindent 12 }}
          env:
            {{- range $key, $val := .Values.llamaParser.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: NGC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "insureguard.nvidiaSecretName" . }}
                  key: NGC_API_KEY
          ports:
            - containerPort: {{ .Values.llamaParser.port }}
              name: http
          volumeMounts:
            - name: nim-cache
              mountPath: /opt/nim/.cache
            - name: dshm
              mountPath: /dev/shm
          resources:
            limits:
              nvidia.com/gpu: "1"
      volumes:
        - name: nim-cache
          emptyDir: {}
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.llamaParser.shmSize }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "insureguard.fullname" . }}-llama-parser
  labels:
    {{- include "insureguard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.llamaParser.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: llama-parser
    app.kubernetes.io/instance: {{ .Release.Name }}
